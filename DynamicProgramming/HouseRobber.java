package DynamicProgramming;
import java.util.*;
/**
 * Given an array arr[houses] ,what is the maximum amount a thief can rob without robbing two consecutive indices of houses and the
 * houses are in circular fasion
* */
public class HouseRobber {
    public static void main(String[] args) {
        int[]arr={};
        int n=arr.length;
        System.out.println(houseRobber(arr,n));
    }
    private static int houseRobber(int[]arr,int n){
        return 0;
    }
}
